# ============================================================
# Dev Crew — per-agent tokens TEMPLATE.
# Real values go in tokens/tokens.yaml (gitignored).
# Copy: cp tokens/tokens.example.yaml tokens/tokens.yaml
# ============================================================
#
# Each agent gets access to its OWN section only (mounted into its
# container read-only). The same service token can be repeated across
# agents if the service allows it.

agents:
  developer:
    github:
      token: ""            # agent's GITHUB_TOKEN
      username: ""
    llm:
      base_url: ""         # OpenAI-compatible endpoint
      api_key: ""          # LLM provider key
      model: ""            # default model

  qa:
    github:
      token: ""
      username: ""
    llm:
      base_url: ""
      api_key: ""
      model: ""

  tech-pm:
    github:
      token: ""
      username: ""
    llm:
      base_url: ""
      api_key: ""
      model: ""
